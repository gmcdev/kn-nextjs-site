DirectorySlash Off
DirectoryIndex disabled

RewriteEngine on

# Strip trailing slash internally
RewriteRule ^(.+)/$ $1 [L]

# Serve .html file for clean URLs (e.g., music → music.html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]
